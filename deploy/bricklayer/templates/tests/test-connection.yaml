apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bricklayer.fullname" . }}-test-connection"
  labels:
    {{- include "bricklayer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bricklayer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
